# 行程變更範本

出發前要調整「5 個人共用」的行程時使用。Claude 會依內容修改 `data.js` 並 push，所有人打開 app 就會看到新版。

> 旅途中的臨時調整請用 app 內的編輯功能。那些修改只存在自己的手機上，不會同步給其他人。

## 使用方式

1. 把下方「本次變更」區塊填好（用不到的類型整段刪掉；同一類型有多筆就複製多份）
2. 存檔後跟 Claude 說「照行程變更範本改」
3. Claude 改完、push 之後，會把「本次變更」清空，留給下次使用

**小提醒**

- **地點名稱**：照 app 上顯示的名稱寫即可，例如「PATH 荷蘭鬆餅」「明治神宮」
- **交通**：可以留空，Claude 會依前後站補上建議路線。但地鐵路線和時間請再用 Google Maps 確認
- **前後站會自動調整**：移動地點後，前後站的「從哪裡過去」也會一起更新
- **新地點的座標**：在 Google Maps 長按該地點，畫面上方會出現座標（例如 `35.6764, 139.6993`），複製貼上即可

## 更換地點圖片

圖片不用填範本，直接放進資料夾：

1. 每個地點都有自己的資料夾：`images/<地點名稱>/`，例如 `images/淺草寺/`
2. 圖片用**數字**命名：`1.jpg`、`2.jpg`、`3.jpg`⋯，依數字順序顯示，**第 1 張是卡片封面**
3. 要換圖就直接用新照片覆蓋同名檔案；不要的圖直接刪掉
4. 放好後跟 Claude 說「圖片放好了」，Claude 會更新圖片清單（`node tools/sync-images.js`）並 push

**小提醒**

- 建議用橫式照片，寬度 1200px 左右（小畫家「調整大小」）；超過 500KB 會被提醒
- 支援 `.jpg`、`.png`、`.webp`；檔名不是數字的檔案不會顯示
- 資料夾是空的地點，會顯示預設的相片圖示

---

## 本次變更

### 1. 移動地點（改天數或時間）

- 地點：
- 改到：第 _ 天 __:__
- 交通（選填）：

### 2. 候補景點排入行程

- 地點：
- 排到：第 _ 天 __:__
- 交通（選填）：

### 3. 移出行程（改回候補，不刪除）

- 地點：

### 4. 新增地點

- 名稱：
- 日文／英文名稱（選填）：
- 類別：美食／購物／景點／住宿
- 排到：第 _ 天 __:__（或填「候補」）
- 座標：
- Google Maps 連結（選填）：
- 介紹：
- 交通（選填）：

### 5. 修改說明或備註

- 地點：
- 要改的內容：介紹／名稱／交通說明／Google Maps 連結
- 新的內容：

### 6. 刪除地點

- 地點：

### 7. 其他（行前指南、機票、飯店、預算、行前清單等）

- 

---

## 填寫範例

```
### 1. 移動地點（改天數或時間）
- 地點：Shake Shack 新宿
- 改到：第 3 天 12:00
- 交通（選填）：

### 2. 候補景點排入行程
- 地點：明治神宮
- 排到：第 4 天 14:00
- 交通（選填）：從原宿大阪燒走過去

### 6. 刪除地點
- 地點：Lawson 押上店
```
